<?xml version="1.0" encoding="utf-8"?>
<!-- Il codice da far leggere alla cassa: fondo bianco, il codice al centro,
     il nome sopra e il numero sotto, piccoli. Le misure dell'immagine le
     decide CodiceActivity, che conosce il cerchio dello schermo. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:gravity="center"
    android:orientation="vertical">

    <TextView
        android:id="@+id/etichetta"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="48dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="#202124"
        android:textSize="14sp" />

    <ImageView
        android:id="@+id/immagine"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginVertical="6dp"
        android:adjustViewBounds="true"
        android:contentDescription="@null"
        android:scaleType="centerInside" />

    <TextView
        android:id="@+id/codice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="48dp"
        android:ellipsize="middle"
        android:maxLines="1"
        android:textColor="#202124"
        android:textSize="13sp" />

</LinearLayout>
